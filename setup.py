from setuptools import setup, find_packages

setup(
    name="pekousadaunique",
    version="0.2",
    packages=find_packages(),
    long_description="A simple package. use **from earth import helloworld** and helloworld.do_waky_stuff() .",
    long_description_content_type="text/plain",  # or "text/x-rst" for RST
)
